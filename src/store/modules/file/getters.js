const getters = {
    collapse: state => state.collapse,
    imageModel: state => state.imageModel,
    search: state => state.search,
    storage: state => state.storage,
};

export default getters;
